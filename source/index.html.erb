---
pageable: false
---

<% page_articles.each_with_index do |article, i| %>
<article class="list">
  <%= link_to article.title, article %>
  <span class="date">
    <%= article.date.strftime('%b %e') %>
  </span>
</article>
<% end %>
